#include "CameraRender/Camera.hpp"

int main(int args, TCHAR* argv[]) {
	Camera mainCamera;
	mainCamera.Init(800, 600);
	return 0;
}
int _tmain(void) {
	Camera mainCamera;
	mainCamera.Init(800, 600);
	return 0;
}